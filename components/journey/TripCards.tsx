'use client';
import Link from 'next/link';
import type { Booking, Trip, Voucher } from '../../lib/domain/travel';
import { StatusBadge } from './Badges';
export function TripSummaryCard({trip}:{trip:Trip}){return <article className="trip-summary-card"><div className="route-symbol"><b>THR</b><i>✈</i><b>SYZ</b></div><span>{trip.origin} ← {trip.destination}</span><h2>{trip.title}</h2><p>{trip.startDate} تا {trip.endDate} · {trip.durationLabel} · {trip.travellers.length} مسافر</p><StatusBadge status={trip.status}/><Link href={`/trips/${trip.id}`}>ورود به سفر من ←</Link></article>}
export function BookingCard({booking}:{booking:Booking}){const supplierText=booking.supplierLabel.includes('Mock')||booking.supplierLabel.includes('Provider')?'اطلاعات تأمین‌کننده هنوز تأیید نشده است':booking.supplierLabel;return <article className="booking-card"><div><small>شماره پیگیری</small><code>{booking.reference.replace('-MOCK','')}</code></div><h3>{booking.title}</h3><p>{supplierText}</p><StatusBadge status={booking.status}/>{booking.status==='confirmed'&&<Link className="manage-booking-link" href={`/manage-booking/${booking.id}`}><b>تغییر، کنسلی و استرداد</b><small>قوانین رزروت را ببین و درخواستت را مدیریت کن</small></Link>}</article>}
export function VoucherCard({voucher}:{voucher:Voucher}){return <article className="voucher-card" id="voucher"><div><span>واچر کارن‌سیر</span><StatusBadge status={voucher.status}/></div><h3>{voucher.title}</h3><p>شناسه: {voucher.id}</p><div className="voucher-actions"><button type="button" onClick={()=>window.print()}>مشاهده / چاپ واچر</button><Link href={`/manage-booking/${voucher.bookingId}`}>مدیریت رزرو</Link></div><small>این واچر نمایشی و برای استفاده در سفر واقعی نامعتبر است.</small></article>}
