import Link from 'next/link';
import { JourneyShell, MockNotice } from '../../../components/journey/JourneyShell';
import { mockServices, mockTrip } from '../../../lib/domain/mock-data';
export default function ConfirmationPage(){return <JourneyShell><main className="confirmation-main"><section className="confirmation-card"><div className="success-mark">✓</div><span>تأیید نمایشی</span><h1>رزرو شما با موفقیت ثبت شد</h1><p>سفر تهران به شیراز برای ۲ مسافر به Trip Context نمونه اضافه شد.</p><MockNotice>هیچ پرداخت، رزرو یا واچر واقعی انجام نشده است.</MockNotice><div><Link href={`/trips/${mockTrip.id}#voucher`}>مشاهده بلیت / واچر</Link><Link className="primary-trip-action" href={`/trips/${mockTrip.id}`}>ورود به سفر من</Link><Link href="/manage-booking/book1">تغییر، کنسلی و استرداد</Link></div></section><section className="complete-trip"><header><span>گام بعدی با انتخاب شما</span><h2>حالا سفرت را کامل کن</h2><p>انتقال خودکار انجام نمی‌شود؛ فقط خدمات خریداری‌نشده مرتبط با شیراز نمایش داده شده‌اند.</p></header><div>{mockServices.slice(0,5).map(service=><Link href={`/services?destination=shiraz&focus=${service.id}`} key={service.id}><span>{service.action}</span><h3>{service.title}</h3><p>{service.description}</p><small>پیشنهاد Mock ←</small></Link>)}</div></section></main></JourneyShell>}
